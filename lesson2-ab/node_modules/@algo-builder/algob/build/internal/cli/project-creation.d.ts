import type { PromiseAny } from "../../types";
export declare function pkgManager(isNpm: boolean): string;
export declare function printWelcomeMessage(): Promise<void>;
export declare function printSuggestedCommands(isNpm: boolean): void;
export declare function createProject(location: string, isTSProject: boolean, withInfrastucture: boolean, isNpm: boolean): PromiseAny;
export declare function createConfirmationPrompt(name: string, message: string): {
    type: string;
    name: string;
    message: string;
    initial: string;
    default: string;
    isTrue(input: string | boolean): boolean;
    isFalse(input: string | boolean): boolean;
    format(): string;
};
export declare function installDependencies(packageManager: string, args: string[], location?: string): Promise<boolean>;
